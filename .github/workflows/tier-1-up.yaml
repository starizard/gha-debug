name: "tier 1 up"
on:
  push:
    branches:
      - "main"
    paths:
      - cloud/**
permissions:
  pull-requests: write
  contents: read
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
